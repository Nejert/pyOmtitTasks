word = "Тося"

for c in word:
    print(c)

print()

for i in range(len(word)):
    print(word[i])

#word = 'Лутший'
#word_list = list(word)
#word_list[2] = 'ч'  # исправим ошибку, заменив элемент списка другим символом
#print(word_list)

#word = str(word_list)  # попытка объединить список символов в строку
#print(word)  # вывод полученного объекта
#word = ''.join(word_list)  # использование метода строк "join" для объединения списка символов в строку
#print(word)  # вывод полученного объекта